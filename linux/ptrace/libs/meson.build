

libhex = shared_library('hex', 'hexmem/hex.c', 'hexmem/hex.h', include_directories: inc, install : true)

libhex_header = install_headers('hexmem/hex.h')

inc = [inc , include_directories('hexmem')]